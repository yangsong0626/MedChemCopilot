# Frontend

React + TypeScript frontend for medicinal chemistry design review.

## Run

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` to point at the backend if it is not running on
`http://127.0.0.1:8000`.

